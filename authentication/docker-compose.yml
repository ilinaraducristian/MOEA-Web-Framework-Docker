version: "3"
services:
  keycloak:
    image: jboss/keycloak:11.0.0
    ports:
      - "8180:8080"
    environment:
      - "KEYCLOAK_USER=admin"
      - "KEYCLOAK_PASSWORD=secure_password"
      - "KEYCLOAK_IMPORT=/tmp/MOEA-Web-Framework-realm.json"
    volumes:
      - ./MOEA-Web-Framework-realm.json:/tmp/MOEA-Web-Framework-realm.json
  keycloak_another_issuer:
    image: jboss/keycloak:11.0.0
    ports:
      - "8280:8080"
    environment:
      - "KEYCLOAK_USER=admin"
      - "KEYCLOAK_PASSWORD=secure_password"
      - "KEYCLOAK_IMPORT=/tmp/MOEA-Web-Framework-realm.json"
    volumes:
      - ./MOEA-Web-Framework-realm.json:/tmp/MOEA-Web-Framework-realm.json
